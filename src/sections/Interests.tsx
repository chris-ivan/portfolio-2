import Frame from "../components/template/Frame";
import { FRAMES } from "../static/frames";

const Interests = () => {
  return (
    <Frame {...FRAMES.INTERESTS}>
      <h1>Interests</h1>
    </Frame>
  );
};

export default Interests;
