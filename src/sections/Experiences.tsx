import Frame from "../components/template/Frame";
import { FRAMES } from "../static/frames";

const Experiences = () => {
  return (
    <Frame {...FRAMES.EXPERIENCES}>
      <h1>Experiences</h1>
    </Frame>
  );
};

export default Experiences;
