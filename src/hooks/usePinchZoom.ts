// import { useEffect } from "react";

const usePinchZoom = () => {
  // const handleZoom = (event: ) => {
  //   console.log(event);
  // };
  // useEffect(() => {
  //   window.addEventListener("gesturechange", handleZoom);
  //   return () => {
  //     window.removeEventListener("touchstart", handleZoom);
  //   };
  // }, []);
};

export default usePinchZoom;
